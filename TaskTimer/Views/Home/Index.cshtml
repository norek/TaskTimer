@{
    ViewBag.Title = "Home Page";
}

<div class="row" ng-controller="HomeController">
    <form class="form-inline">
        <div class="form-group">
            <div class="input-group">
                <input type="text" class="form-control" placeholder="Type event description" ng-model="currentEvent.Description">
                <span class="input-group-addon">{{ currentEvent.ElapsedTime}}</span>
                <div class="input-group-btn">
                    <div class="btn-group">
                        <div class="dropdown">
                            <button class="btn btn-danger dropdown-toggle" data-toggle="dropdown" type="button">PROJECTS<span class="caret"></span></button>
                            <ul class="dropdown-menu">
                                <li ng-repeat="project in projects">{{project.Name}} {{project.Id}}</li>
                            </ul>
                        </div>
                    </div>
                    <button class="btn" ng-class="{'btn-danger': currentEvent.IsRunning, 'btn-success': !currentEvent.IsRunning}" type="button" ng-click="ToggleEvent()">{{ToggleEventButtonCaption}}</button>
                </div>
            </div>
        </div>
    </form>

    @*<table class="table table-hover">
        <thead>
        <tr>
            <th>Project</th>
            <th>Description</th>
            <th>Start date</th>
            <th>Finish date</th>
        </tr>
        </thead>
        <tr ng-repeat="historyItem in historyList">
            <td>{{historyItem.Name }}</td>
            <td>{{historyItem.Description }}</td>
            <td>{{historyItem.StartDate | jsonDate:'yyyy-MM-dd HH:mm:ss'}}</td>
            <td>{{historyItem.FinishDate | jsonDate:'yyyy-MM-dd HH:mm:ss'}}</td>
        </tr>c
    </table>*@

    <div ng-view></div>

</div>
<nav class="navbar navbar-default navbar-fixed-bottom navbar-inverse">

    <div id="period_progress" class="container">
        <div class="row">
            <div class="col-md-9">
                <div class="progress">
                    <div class="progress-bar" role="progressbar" aria-valuenow="60" aria-valuemin="0" aria-valuemax="100" style="width: 60%;">
                        60%
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <label>Total week: 12h</label>
            </div>
        </div>
    </div>

</nav>

